# Oscar Whitehill Portfolio

Upload everything in this folder to the root of the GitHub repository. Keep the `assets` and `projects` folders intact.

After uploading, open repository **Settings → Pages**, choose **Deploy from a branch**, select `main` and `/ (root)`, then save.

Replace the placeholder `#` links in `index.html` with the real ArtStation, LinkedIn and CV links.
